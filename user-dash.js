// Function to select a provider
function selectProvider(providerName) {
    alert("You have selected " + providerName); 
    // Show alert with provider name
     window.location.href = "dashboard.html";
}
